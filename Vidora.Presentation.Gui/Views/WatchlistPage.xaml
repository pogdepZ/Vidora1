<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Vidora.Presentation.Gui.Views.WatchlistPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Vidora.Presentation.Gui.Views"
    xmlns:models="using:Vidora.Presentation.Gui.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="WatchlistPageRoot">

    <Page.Resources>
        <!-- Movie ItemTemplate -->
        <DataTemplate x:Key="MovieItemTemplate" x:DataType="models:Movie">
            <Grid Width="200">
                <Button
                    Command="{Binding ViewModel.ViewMovieDetailCommand, ElementName=WatchlistPageRoot}"
                    CommandParameter="{x:Bind}"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Padding="0"
                    Background="Transparent"
                    BorderThickness="0"
                    CornerRadius="12">
                    <Grid 
                        CornerRadius="12" 
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="280"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Poster -->
                        <Grid Grid.Row="0" CornerRadius="12,12,0,0">
                            <Image Source="{x:Bind PosterUrl}" Stretch="UniformToFill"/>
                            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" 
                                    Visibility="{x:Bind PosterUrl, Converter={StaticResource NullToVisibilityConverter}}">
                                <FontIcon Glyph="&#xE8B2;" FontSize="48" Foreground="{ThemeResource TextFillColorTertiaryBrush}"/>
                            </Border>
                            <Border CornerRadius="12,12,0,0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="Transparent" Offset="0.6"/>
                                        <GradientStop Color="#80000000" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>

                            <!-- Rating Badge -->
                            <Border Background="#E50914" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <FontIcon Glyph="&#xE735;" FontSize="12" Foreground="White"/>
                                    <TextBlock Text="{x:Bind AvgRating}" FontSize="12" FontWeight="SemiBold" Foreground="White"/>
                                </StackPanel>
                            </Border>

                            <!-- Year Badge -->
                            <Border Background="#99000000" CornerRadius="6" Padding="8,4" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
                                <TextBlock Text="{x:Bind ReleaseYear}" FontSize="12" FontWeight="Medium" Foreground="White"/>
                            </Border>

                            <!-- Heart Icon -->
                            <Border Background="#CC000000" CornerRadius="20" Padding="8" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10">
                                <FontIcon Glyph="&#xEB52;" FontSize="14" Foreground="#E50914"/>
                            </Border>

                            <!-- Remove Button -->
                            <Button Command="{Binding ViewModel.RemoveFromWatchlistCommand, ElementName=WatchlistPageRoot}"
                                    CommandParameter="{x:Bind}"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Bottom"
                                    Margin="10"
                                    Padding="10"
                                    CornerRadius="20"
                                    Background="#CC000000"
                                    ToolTipService.ToolTip="Xóa khỏi yêu thích">
                                <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="White"/>
                            </Button>
                        </Grid>

                        <!-- Info -->
                        <StackPanel Grid.Row="1" Padding="12,12" Spacing="6">
                            <TextBlock Text="{x:Bind Title}" FontWeight="SemiBold" FontSize="14" TextTrimming="CharacterEllipsis" MaxLines="2" TextWrapping="Wrap" Height="40" ToolTipService.ToolTip="{x:Bind Title}"/>
                            <TextBlock Text="{x:Bind GenresDisplay}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextTrimming="CharacterEllipsis" MaxLines="1"/>
                        </StackPanel>
                    </Grid>
                </Button>
            </Grid>
        </DataTemplate>

        <!-- GenreGroup ItemTemplate -->
        <DataTemplate x:Key="GenreGroupItemTemplate" x:DataType="models:GenreMovieGroup">
            <StackPanel Spacing="16">
                <!-- Genre Header -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="12">
                        <Border Background="#E50914" CornerRadius="4" Padding="14,8">
                            <TextBlock Text="{x:Bind GenreName}" FontSize="18" FontWeight="Bold" Foreground="White"/>
                        </Border>
                        <TextBlock VerticalAlignment="Center" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="{x:Bind Movies.Count}"/>
                            <Run Text="phim"/>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4" VerticalAlignment="Center" Opacity="0.6">
                        <TextBlock Text="Kéo để xem thêm" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                        <FontIcon Glyph="&#xE76C;" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                </Grid>

                <!-- Horizontal Movie List -->
                <Grid>
                    <ScrollViewer HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Hidden" VerticalScrollMode="Disabled" VerticalScrollBarVisibility="Disabled" Padding="0,8,0,8">
                        <ItemsRepeater ItemsSource="{x:Bind Movies}" ItemTemplate="{StaticResource MovieItemTemplate}">
                            <ItemsRepeater.Layout>
                                <StackLayout Orientation="Horizontal" Spacing="16"/>
                            </ItemsRepeater.Layout>
                        </ItemsRepeater>
                    </ScrollViewer>

                    <!-- Left fade -->
                    <Border HorizontalAlignment="Left" Width="40" IsHitTestVisible="False">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="{ThemeResource ApplicationPageBackgroundThemeBrush}" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>

                    <!-- Right fade -->
                    <Border HorizontalAlignment="Right" Width="60" IsHitTestVisible="False">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="Transparent" Offset="0"/>
                                <GradientStop Color="{ThemeResource ApplicationPageBackgroundThemeBrush}" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>
                </Grid>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Hidden">
            <StackPanel Padding="24" Spacing="24">
                <!-- Header -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Spacing="4">
                        <TextBlock Text="Phim yêu thích" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                            <Run Text="{x:Bind ViewModel.TotalMovies, Mode=OneWay}"/>
                            <Run Text="phim"/>
                        </TextBlock>
                    </StackPanel>

                    <Button Grid.Column="1" Command="{x:Bind ViewModel.LoadWatchlistCommand}" VerticalAlignment="Center" Padding="12,8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                            <TextBlock Text="Làm mới"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- Loading State -->
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12" Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ProgressRing IsActive="True" Width="48" Height="48"/>
                    <TextBlock Text="Đang tải danh sách yêu thích..." Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>

                <!-- Empty State -->
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="16" Padding="48" Visibility="{x:Bind ViewModel.HasMovies, Mode=OneWay, Converter={StaticResource BoolNegationToVisibilityConverter}}">
                    <StackPanel Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource BoolNegationToVisibilityConverter}}">
                        <FontIcon Glyph="&#xEB52;" FontSize="64" Foreground="{ThemeResource TextFillColorTertiaryBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Chưa có phim yêu thích" FontSize="20" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,16,0,0"/>
                        <TextBlock Text="Thêm phim vào danh sách yêu thích để xem sau" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>

                <!-- Genre Groups -->
                <ItemsRepeater ItemsSource="{x:Bind ViewModel.GenreGroups, Mode=OneWay}" ItemTemplate="{StaticResource GenreGroupItemTemplate}" Visibility="{x:Bind ViewModel.HasMovies, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <ItemsRepeater.Layout>
                        <StackLayout Orientation="Vertical" Spacing="40"/>
                    </ItemsRepeater.Layout>
                </ItemsRepeater>

                <!-- Bottom padding -->
                <Border Height="40" Visibility="{x:Bind ViewModel.HasMovies, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
